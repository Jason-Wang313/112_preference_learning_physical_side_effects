# Plan

Build paper 112 `preference_learning_physical_side_effects` from the shared pool, compile PDF to Downloads only, and publish the exact-name public repo.

Continuation rule for v4.1:
- first write the paper-specific ICLR execution plan;
- rerun the experiment with low-RAM thread caps;
- expand recoverable stress evidence to task/regime/seed rows;
- document at least eight concrete failure boundaries;
- audit the strongest non-oracle baseline, paired seeds, ablations, stress sweep, numeric integrity, and artifact location;
- keep the terminal decision honest: `STRONG_REVISE` unless real human-label and robot/high-fidelity evidence exist.
